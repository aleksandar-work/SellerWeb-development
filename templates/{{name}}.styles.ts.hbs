import { I{{name}}StyleProps, I{{name}}Styles } from "./{{name}}.types";

// unused placeholder props must starts with _ because of eslint rule, change it when used
export function getStyles(_props: I{{name}}StyleProps): I{{name}}Styles {
    return {};
}
